{
  "default": {
    "default": {
      "username": "<%= @username %>"
      "password": "<%= @password %>"
      "database": "<%= @database %>"
      "host": "localhost"
      "port": "''"
      "driver": "mysql"
      "prefix": "''"
    }
  }
}
